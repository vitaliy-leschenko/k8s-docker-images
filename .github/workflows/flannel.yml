name: flannel-images

on:
  push:

jobs:
  build:
    runs-on: ubuntu-20.04
    defaults:
      run:
        shell: pwsh
    steps:
    - uses: actions/checkout@v2
    - name: Build images
      run: cd ./kubeadm/flannel && ./build.ps1
